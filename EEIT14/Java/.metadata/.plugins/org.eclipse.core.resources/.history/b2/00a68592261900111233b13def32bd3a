package com.example.WebSocket.service;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.example.WebSocket.entity.Shoppingcart;
import com.example.WebSocket.repo.ShoppingcartRepo;
import com.example.WebSocket.util.BCrypt;


@Service
public class ShoppingcartService {
	@Autowired
	private ShoppingcartRepo cart;
	
	public void order() {
		Shoppingcart cart = new Shoppingcart();
		
		cart.setId(null);
		cart.setOrderId(null);
		cart.setCoursesId(null);
		cart.setUnitPrices(null);
		cart.setLessonCount(null);
		cart.setDiscountPrice(null);
	}
}

//TODO 計算訂單總金額
