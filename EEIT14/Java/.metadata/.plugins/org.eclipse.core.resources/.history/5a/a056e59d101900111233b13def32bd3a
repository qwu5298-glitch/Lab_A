package com.example.WebSocket.service;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.example.WebSocket.entity.Shoppingcart;
import com.example.WebSocket.repo.ShoppingcartRepo;
import com.example.WebSocket.util.BCrypt;


@Service
public class ShoppingcartService {
	@Autowired
	private ShoppingcartRepo shoppingcartRepo;
	
	public void order() {
		shoppingcartRepo.;
		shoppingcartRepo.setCoursesId(5L);
		shoppingcartRepo.setMerchantTradeNo("TX" + System.currentTimeMillis());
		shoppingcartRepo.setTotalAmount(1200);
	}
}

//TODO 計算訂單總金額
